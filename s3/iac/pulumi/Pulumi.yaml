name: s3-simple-sdk
description: A py project for AWS S3 to create a simple bucket.
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
