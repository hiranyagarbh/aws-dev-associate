#!/usr/bin/env bash
set -e

root_path=$(realpath .)
echo "== SAM deploy..."

sam deploy \
  --template-file "./.aws-sam/build/template.yaml" \
  --stack-name "inline-lambda-py" \
  --capabilities "CAPABILITY_IAM"
