#!/usr/bin/env bash
echo "== SAM deploy..."

config_file="$(realpath .)/samconfig.toml"

sam deploy \
  --template-file "./.aws-sam/build/template.yaml" \
  --stack-name "zip-local-lambda-py" \
  --capabilities "CAPABILITY_IAM" \
  --config-file "$config_file"
